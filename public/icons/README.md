# Icons

Drop SVG assets here. They are served at `/icons/<filename>`.

- **solana.svg** — Used on the drop page for the "Solana" badge. Recommended: 24×24 or 32×32 viewBox; the UI displays it at 16×16px (`w-4 h-4`).
